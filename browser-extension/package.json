{
  "name": "@awareness-sdk/browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Awareness Memory Bridge — MV3 browser extension that auto-captures ChatGPT/Gemini/Doubao/Kimi/DeepSeek Q&A into the local Awareness daemon and injects relevant memories back.",
  "type": "module",
  "scripts": {
    "verify:io": "node ../../scripts/verify-extension-io-boundary.mjs",
    "test:e2e": "playwright test",
    "lint:manifest": "node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8'));console.log('manifest ok')\""
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
